{
  "name": "sails-generate-humpback-model",
  "version": "0.10.0",
  "description": "sails generate humpback-model",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "humpback-model",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "balderdashy",
  "license": "MIT",
  "dependencies": {
  	"lodash": ">=2.4.x",
  	"merge-defaults": ">=0.1.0"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "humpback-model",
    "behavior": "overrides `sails generate humpback-model`",
    "sailsVersion": "~0.10.0"
  }
}
