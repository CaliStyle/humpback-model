{
  "name": "sails-generate-humpback-model",
  "version": "0.11.1",
  "description": "sails generate humpback-model",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "humpback-model",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "scottBwyatt",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/CaliStyle/humpback-model.git"
  },
  "dependencies": {
    "lodash": ">=2.4.x",
    "merge-defaults": ">=0.1.0"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "humpback-model",
    "behavior": "overrides `sails generate humpback-model`",
    "sailsVersion": "~0.10.0"
  }
}
